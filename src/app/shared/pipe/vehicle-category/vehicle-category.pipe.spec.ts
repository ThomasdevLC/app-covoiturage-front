import { VehicleCategoryPipe } from './vehicle-category.pipe';

describe('VehicleCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new VehicleCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
