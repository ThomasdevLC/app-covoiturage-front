import { LicensePlateDirective } from './license-plate.directive';

describe('LicensePlateDirective', () => {
  it('should create an instance', () => {
    // @ts-ignore
    const directive = new LicensePlateDirective();
    expect(directive).toBeTruthy();
  });
});
